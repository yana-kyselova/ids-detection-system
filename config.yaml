mode: live          # live | pcap
iface:        # only for live mode
pcap_path: samples/test.pcap

whitelist:
  ips: ["127.0.0.1"]
  macs: []

thresholds:
  rate_threshold_pps: 500

  syn_flood_syn_per_window: 120
  syn_window_sec: 10 

  portscan_ports_per_window: 25
  portscan_window_sec: 30

  arp_mac_changes_per_window: 2
  arp_window_sec: 60

output:
  alerts_log: alerts.log
  events_jsonl: events.jsonl
  report_txt: report.txt